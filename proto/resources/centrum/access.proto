syntax = "proto3";

package resources.centrum;

import "resources/timestamp/timestamp.proto";
import "validate/validate.proto";

option go_package = "github.com/fivenet-app/fivenet/v2025/gen/go/proto/resources/centrum;centrum";

enum AccessLevel {
  ACCESS_LEVEL_UNSPECIFIED = 0;
  ACCESS_LEVEL_BLOCKED = 1;
  ACCESS_LEVEL_VIEW = 2;
  ACCESS_LEVEL_PARTICIPATE = 3;
  ACCESS_LEVEL_MANAGE = 4;
}

// @dbscanner: json
message Access {
  repeated JobAccess jobs = 1 [(validate.rules).repeated.max_items = 10]; // @gotags: alias:"job_access"
}

message JobAccess {
  uint64 id = 1;
  optional resources.timestamp.Timestamp created_at = 2;
  uint64 target_id = 3;
  string job = 4 [(validate.rules).string.max_len = 20];
  optional string job_label = 5 [(validate.rules).string.max_len = 50];
  int32 minimum_grade = 6 [(validate.rules).int32.gte = 0];
  optional string job_grade_label = 7 [(validate.rules).string.max_len = 50];
  AccessLevel access = 8 [(validate.rules).enum.defined_only = true];
}

// Dummy - DO NOT USE!
message UserAccess {}

// Dummy - DO NOT USE!
message QualificationAccess {}

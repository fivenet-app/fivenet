syntax = "proto3";

package resources.filestore;

import "validate/validate.proto";

option go_package = "github.com/galexrt/fivenet/gen/go/proto/resources/filestore;filestore";

message File {
  optional string url = 1 [(validate.rules).string.max_len = 128];
  optional string type = 2 [(validate.rules).string.max_len = 4];
  bytes data = 3 [(validate.rules).bytes.max_len = 2097152];
}

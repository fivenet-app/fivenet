import { DocumentAccess } from '~~/gen/ts/resources/documents/documents';
import { AccessLevel } from '~~/gen/ts/resources/jobs/qualifications';
import { User } from '~~/gen/ts/resources/users/users';

export function checkDocAccess(docAccess: DocumentAccess, activeChar: User, level?: AccessLevel): boolean {
    // TODO

    return false;
}

import { ExclamationIcon, HelpIcon, MedicalBagIcon, PistolIcon, PoliceBadgeIcon } from 'mdi-vue3';

export const markerIcons = markRaw([HelpIcon, ExclamationIcon, MedicalBagIcon, PoliceBadgeIcon, PistolIcon]);

package auth

const (
	// Cookie used to represent account user is logged in as.
	AccCookieName = "fivenet_acc"
	// Cookie used to represent if user is authenticated (true/false), for frontend use.
	AuthedCookieName = "fivenet_authed"
	// Cookie used to represent the (active) user.
	UserCookieName = "fivenet_usr"
)

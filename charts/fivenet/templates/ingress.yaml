{{- if .Values.fivenet.ingress.enabled -}}
{{- $fullName := include "fivenet.fullname" . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "fivenet.labels" . | nindent 4 }}
  {{- with .Values.fivenet.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.fivenet.ingress.className }}
  {{- if .Values.fivenet.ingress.tls }}
  tls:
    {{- range .Values.fivenet.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.fivenet.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: 8080
          {{- end }}
    {{- end }}
{{- end }}
